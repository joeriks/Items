'use strict';

angular.module('itemsApp')
  .controller('MainCtrl', function ($scope, mobileServiceClient) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];

        

  });
